# spark-deno-test

To install dependencies:

```bash
deno install
```

To run:

```bash
deno task start
```

To test:

```bash
deno test
```

> This project was created using `deno init` in bun v2.1.2.
